defmodule StoneChallengeTest do
  use ExUnit.Case
  doctest StoneChallenge

  test "greets the world" do
    assert StoneChallenge.hello() == :world
  end

  test "plano" do
    # tornar get_content e sum_items_value acessivel
    # fazer teste para os possiveis erros de get_content
    # somar os valores da lista resultante da main e comparar com o somatorio
    assert 0 == 0
  end
end
